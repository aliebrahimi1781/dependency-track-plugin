Partie extraction des composants :

Facultatif : Remettre à jour la base de données des CPE : CPE.json
1) Mettre les bases de données XML des CVE de NVD dans le dossier CVE
2) Mettre la base de données XML des CPE de NVD dans le dossier CPE
3) Exécuter les scripts "extract_Official_CPE.py" puis "extract_noOfficial_CPE.py".

Puis:

4) Mettre les filepath des programmes à analyser dans le fichier "executables.txt"

Exemples :
/usr/bin/dig
/usr/sbin/cupsfilter
/usr/sbin/grub-install

5) Executer le script "Main.py"

Un fichier en format JSON "program_components" est alors créé dans le dossier "Pluggin/dependency-track".
C'est un dictionnaire de la forme : {"dwp": [["gnu", "glibc", "2.19"], ["gnu", "zlib", "1.1"]]}

Il faut ensuite enregistrer les données de ce dictionnaire dans la base de données gérée par Dependency-Track...

